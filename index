<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta property="og:image" content="https://amwcdn.com/featured/345283/responsive-images/76336e23bf___full-webp_709_494.webp"/>
    <meta property="og:title" content="مفاجئات صادمة وتفاصيل مثيرة"/>
    <meta property="og:description" content="فى محاكمة سفاح التجمع"/>
    <title>We-Choses-To-You</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            direction: rtl; /* لجعل النص من اليمين إلى اليسار */
        }

        .container {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-around;
            padding: 20px;
        }

        .board {
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
            margin: 20px;
            max-width: 300px;
            text-align: center;
            transition: transform 0.3s;
            flex: 1 1 calc(33.333% - 40px); /* 3 boards لكل صف */
            box-sizing: border-box;
        }

        .board:hover {
            transform: translateY(-10px);
        }

        .board img {
            width: 100%;
            border-top-left-radius: 8px;
            border-top-right-radius: 8px;
        }

        .board h3 {
            margin: 15px 0;
        }

        .board p {
            padding: 0 15px 15px;
            color: #555;
        }

        .board a {
            text-decoration: none;
            color: inherit;
        }

        @media (max-width: 768px) {
            .board {
                flex: 1 1 calc(50% - 40px); /* 2 boards لكل صف على الشاشات الصغيرة */
            }
        }

        @media (max-width: 480px) {
            .board {
                flex: 1 1 100%; /* 1 board لكل صف على الهواتف */
            }
        }
    </style>
</head>
<body>
    <div class="container"></div>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const links = [
                'https://bit.ly/Testlink1amwaly'
            ];

            const container = document.querySelector('.container');
            const scriptURL = 'https://script.google.com/macros/s/AKfycbxeke0k8WNqaU6Gw1BdfpegcLoLZ-4mzifKN9Zujb2p2nq2ALPsRaJnMZhoOZT-bGZLSw/exec'; // استبدل هذا بالـ URL الخاص بتطبيق Google Apps Script

            links.forEach(link => {
                fetch(`${scriptURL}?url=${encodeURIComponent(link)}`)
                    .then(response => response.json())
                    .then(data => {
                        const board = document.createElement('div');
                        board.classList.add('board');
                        board.innerHTML = `
                            <a href="${link}" target="_blank">
                                <img src="${data.image}" alt="${data.title}">
                                <h3>${data.title}</h3>
                                <p>${data.description}</p>
                            </a>
                        `;
                        container.appendChild(board);
                    })
                    .catch(error => console.error('Error fetching the article:', error));
            });
        });
    </script>
</body>
</html>
